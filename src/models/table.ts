export class Column {
    name: string;

    constructor(name: string) {
        this.name = name;
    }
}

export class Table {
    name: string;

    constructor(name: string) {
        this.name = name;
    }
}